builded API using express: 
  npm install express

to avoid mistakes with cors, use: 
  npm install cors

npm install dotenv --save

for tolkien creation and hash: 
  npm install jsonwebtoken,
  npm install bcrypt
  
We use a ORM for Oracle database:
  npm install --save oracledb 
